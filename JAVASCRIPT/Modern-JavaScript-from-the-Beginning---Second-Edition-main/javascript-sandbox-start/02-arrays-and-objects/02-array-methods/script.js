const arr = [34,55,642,341,2];
// Push push it to the end
// Pop pops from end
// unshift add to start
// shift remover from start
// reverse it reverses


// includes -- Check if it includes a value
// Index of -- gives a index of specific value
// Slice -- same as substring and the sring remains  unchanged
// Splice -- same but it slices the array
// Splice -- to remove one element(4,1) fourt element and only one element
///
console.log(arr);